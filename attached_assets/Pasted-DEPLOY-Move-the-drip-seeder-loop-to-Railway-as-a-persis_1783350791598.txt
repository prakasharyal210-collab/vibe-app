DEPLOY: Move the drip seeder loop to Railway as a persistent worker
The LOOP-mode seeder (scripts/src/seed-content.ts) currently runs in the Replit shell and dies when the workspace sleeps. Make it run permanently on Railway. Requirements:

Recommend and implement the best Railway shape for this monorepo. Options to evaluate: (a) a second Railway service in the same project pointing at the same GitHub repo with a start command like pnpm --filter @workspace/scripts run seed-content and worker-appropriate config, or (b) fold it into the existing api-server service as a child process — DON'T do (b), a crashing seeder must never take the API down; go with (a) a separate worker service. Prepare whatever the repo needs for that: ensure scripts/ package builds/runs from a clean install (tsx as a proper dependency not devDependency if needed for production mode, or add a build step), and add a Procfile-style/railway config or document the exact Start Command + Root Directory settings I'll enter in the Railway dashboard.
State persistence — critical change: the loop currently persists queue/progress/image-ledger to local JSON files (seed-content-batch.json, seed-progress.json, seed-used-images.json). Railway containers have EPHEMERAL filesystems — every redeploy wipes them, which would break the never-repeat-an-image guarantee. Migrate state to the database: create scripts/seed-state-migration.sql (OUTPUT ONLY, do not run) with a small table e.g. seeder_state (key text primary key, value jsonb, updated_at) storing the three states as rows — RLS enabled + service-role policy like other tables. Update the seeder to read/write state via Supabase (service key — this script is server-side, direct supabase-js is acceptable here OR route through api-server endpoints, your call, justify it) with the local-file mode kept as fallback when DB env is absent (Replit dev use).
Env vars the worker needs (I'll set them in the Railway service): LOOP=true, API_URL=https://vibe-app-production-d1d9.up.railway.app, PEXELS_API_KEY, ANTHROPIC_API_KEY, SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY (for state). List the exact final set.
Worker hygiene: on SIGTERM (Railway redeploys), finish/abort the in-flight post cleanly and save state; add a tiny health log line every 10 min so Railway logs show liveness; posting-hours window support via env (e.g. POST_HOURS_UTC=22-13 meaning quiet during Nepal/AU night — pick a sensible default so the feed sleeps like humans do).

Typecheck, report: the Railway service settings I need to enter (root dir, start/build commands), the SQL file path for review, the env var list, and what changed in the seeder. Do not push.