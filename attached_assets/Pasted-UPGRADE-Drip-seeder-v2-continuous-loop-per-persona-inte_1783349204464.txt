UPGRADE: Drip seeder v2 — continuous loop, per-persona intervals, guaranteed-unique 4K images, category hashtags
Evolve scripts/src/seed-content.ts (+ generator) from one-shot batch into a continuous feed engine:

Per-persona posting rhythm: each of the 15 personas gets their OWN interval personality, assigned at startup — e.g. some post every ~35-50 min, some every ~1.5-2.5 hrs, a couple of frequent posters at ~20-30 min — each with random jitter per post. Global guardrail: across ALL personas combined, minimum ~8 minutes between any two posts (stagger collisions), and roughly 25-45 posts per 24h total. Different accounts posting at different natural tempos = human.
Continuous loop mode (LOOP=true): the script never "finishes a batch" — when generated content runs low (< 10 unposted items), it automatically calls the generation step (Claude via ANTHROPIC_API_KEY) to produce a fresh batch of global photo posts + occasional poll (1 poll per ~20 posts), appends to the queue, and keeps going. Content rules from v2 apply: fully global, zero Nepal, short vibey captions, every post has imageQuery + fallbacks across the category spread.
Image uniqueness — hard guarantee: maintain scripts/seed-used-images.json recording every Pexels photo ID ever posted. Before posting, skip any candidate photo whose ID is in the ledger; if all top candidates are used, advance the fallback query chain; if the whole chain is exhausted, requery with page=2+. A photo ID is written to the ledger only after successful post. No image ever posts twice, across all runs, forever.
Hashtags: every caption ends with 1-3 category-appropriate hashtags (#food #foodie / #nature #landscape / #fitness #gymlife / #travel #wanderlust / #music / #fashion #style / #tech / #photography etc.) — generator includes them in captions naturally (they flow through the existing hashtag-extraction on the posts endpoint, making the app's hashtag pages come alive too).
Ops: progress + queue + image ledger all persisted (survives restarts, resumes cleanly); nohup run command + log tailing + stop instructions; status line each cycle: queue depth, next persona + ETA, total posted. Keep DRIP one-shot and instant modes working.

Typecheck, report: files changed, the exact LOOP run command, how auto-regeneration is triggered and its cost per batch (~tokens), and how uniqueness survives restarts.