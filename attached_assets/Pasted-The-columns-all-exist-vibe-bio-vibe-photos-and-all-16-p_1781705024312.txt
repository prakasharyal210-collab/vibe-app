The columns all exist — vibe_bio, vibe_photos, and all 16+ preference columns are there. So the migration isn't the problem. The data dies somewhere in the save→load path despite the columns being present.
Since the columns exist and Replit AI claims save+load both go through the API server, the bug is now narrow: either the PATCH isn't actually writing these specific columns, or the GET isn't reading them back, or the field-name mapping between the app and the DB columns doesn't match. Time to make Replit AI prove it with real logs, not assumptions.

Prompt for Replit AI:
Find Vibe Settings STILL don't persist after the last fix. User changes settings, taps Save (sees the "saved ✓" toast), navigates away and back — everything is reset to defaults. The DB columns all exist (confirmed: vibe_bio, vibe_photos, vibe_goal_filter, vibe_zodiac, vibe_education, vibe_family_plans, vibe_communication, vibe_love_style, vibe_pets, vibe_drinking, vibe_smoking, vibe_cannabis, vibe_workout, vibe_social_media, vibe_open_to, vibe_languages, vibe_filter_min_photos, vibe_filter_requires_bio, vibe_request_privacy, all present on the profiles table). So this is NOT a missing-column problem.
Stop assuming and trace the actual data with logging:

On Save (PATCH): Log the exact JSON body being sent from the app to /api/users/profile and /api/users/settings. Then in the API server, log the exact object being passed to the Supabase .update(), and log the Supabase response (data + error). Tell me: does the PATCH body actually contain the changed field values? Does Supabase return success or an error? Does the row actually change in the DB after save (query the row immediately after)?
Check field-name mapping: The app's setting names must map to the exact DB column names. For example, does the app send { vibe_bio: "..." } matching the vibe_bio column? Or is it sending a different key (e.g. bio, vibeBio, vibe_biography) that doesn't match any column, so Supabase silently ignores it? List every field the settings screen sends and the exact DB column it maps to — find any mismatches.
Check ALLOWED_PROFILE_KEYS: Earlier you added fields to an ALLOWED_PROFILE_KEYS allow-list in setup.ts. If the PATCH endpoint filters incoming keys against that list and the new vibe_* columns AREN'T in it, every new field gets stripped out before the update — so the save writes nothing. Confirm ALL the vibe_* columns above are in the allow-list.
On Load (GET): Log the raw response from /api/users/profile/:userId and /api/users/settings/:userId when the settings screen mounts. Does the response actually contain the saved vibe_* values? If the values are in the DB but not in the GET response, the SELECT is missing those columns. If they're in the response but the screen still shows defaults, the screen isn't applying them to state on mount.

Please run an actual save→query→load cycle with logging and report: the PATCH body, the Supabase update result, whether the DB row changed, the GET response, and the exact point where the data is lost. Then fix that specific point.
