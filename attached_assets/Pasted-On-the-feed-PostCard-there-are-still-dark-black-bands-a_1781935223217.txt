On the feed PostCard, there are still dark/black bands above and below the post image — thin empty strips between the author header and the top of the image, and between the bottom of the image and the action bar. The image displays correctly, but these dark empty bands make each post look cut-off/unfinished. The user wants the image to sit flush/clean with no dark gaps above or below it.We previously sized the image container to the image's aspect ratio, but bands are still appearing. Please find and remove them:
Check for a fixed-height or min-height on the media container that's taller than the actual image — leaving dark space the image doesn't fill. The container height must equal the image's displayed height exactly (CARD_W / aspectRatio), with no extra.
Check for letterboxing from the loading/placeholder state — if the container starts at a guessed aspect ratio (e.g. the shimmer/placeholder height) and the real image is shorter, a band remains. Make sure the container resizes to the FINAL image height and doesn't keep leftover placeholder height.
Check the image's background color — if the <Image> or its wrapper has a dark/black backgroundColor and the image doesn't perfectly fill it (sub-pixel rounding, or contain with a slight mismatch), the dark background shows as a band. Either ensure the image fully fills the container, or set the wrapper background to match the feed background (not black) so any 1px remnant is invisible.
Check padding/margin on the media wrapper, the row container, and between the header→image and image→action-bar — remove any vertical padding/margin that's creating the dark strips. The image should butt right up against the header above and the action bar below.
If contain is leaving slivers because the container is even slightly off from the image ratio: size the container to exactly match the image so contain has nothing to letterbox, OR use cover so it fills completely (with the 1.25× cap for extreme portraits).
The goal: each feed image sits flush and clean — no dark bands above or below it, image edges meeting the header and action bar directly.Please report what was causing the bands (container height mismatch / placeholder remnant / dark background showing / padding) and confirm the feed images now sit flush with no dark gaps.